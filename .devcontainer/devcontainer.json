{
  "dockerComposeFile": "docker-compose.linux.yml", // Edit here
  "service": "ros",
  "mounts": ["source=${localWorkspaceFolder},target=/workspace,type=bind"],
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-iot.vscode-ros",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}
